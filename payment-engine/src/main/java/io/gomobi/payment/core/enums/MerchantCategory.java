package io.gomobi.payment.core.enums;

/** Mirrors MERCHANT.MERCHANT_CATEGORY. BE = Billing Entity, PGE = Payment Gateway Entity, PGBE = both. */
public enum MerchantCategory {
    BE,
    PGE,
    PGBE
}
